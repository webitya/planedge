import ServicePageTemplate from '@/components/ServicePageTemplate';

export const metadata = {
  title: 'Landscape Design | Planedge Architect',
  description: 'We design landscapes that harmonize nature and architecture — gardens, courtyards, terraces, and open-air zones that serve as peaceful sanctuaries.',
};

export default function LandscapeDesignPage() {
  return <ServicePageTemplate slug="landscape-design" />;
}
