import ServicePageTemplate from '@/components/ServicePageTemplate';

export const metadata = {
  title: 'Consultation | Planedge Architect',
  description: 'Collaborative one-on-one consultation sessions with senior architects — design briefs, material guidance, mood boards, and project phasing tailored to your vision.',
};

export default function ConsultationPage() {
  return <ServicePageTemplate slug="consultation" />;
}
