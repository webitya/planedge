import ServicePageTemplate from '@/components/ServicePageTemplate';

export const metadata = {
  title: 'Renders | Planedge Architect',
  description: 'Hyper-realistic 3D renders translating blueprints into photorealistic visuals perfect for design validation, property marketing, and client presentations.',
};

export default function RendersPage() {
  return <ServicePageTemplate slug="renders" />;
}
